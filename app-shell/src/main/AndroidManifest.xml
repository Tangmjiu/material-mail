<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- 公开 Intent API（Tasker/Termux），文档见 docs/INTENT-API.md -->
        <receiver
            android:name="com.materialmail.appshell.IntentApiReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.materialmail.action.SYNC_NOW" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.materialmail.appshell.NotificationActionReceiver"
            android:exported="false" />

        <receiver
            android:name="com.materialmail.appshell.widget.InboxWidgetReceiver"
            android:exported="true"
            android:label="Material Mail 收件箱">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/inbox_widget_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>